name: Code Server

on:
  push:
    paths:
      - 'code-server/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build the image
        run: (cd code-server; docker build .)
