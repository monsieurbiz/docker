# Hologram is used to build CSS documentation

## Usage

```
docker run --rm -ti -v `pwd`:/work monsieurbiz/hologram -c hologram_config.yml
```

You can run this where your `hologram_config.yml` is located.

